@EndUserText.label: 'Access Control - PFCG'
@MappingRole: true
define role ZAC_01_AIRLINE_0631 {
    grant  select on ZI_16_AIRLINE_0631
                    where
                          currcode = 'EUR' and
                          (carrid) = aspect pfcg_auth(S_CARRID, carrid, actvt = '03') ;                    
                        
}